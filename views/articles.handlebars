<div class="col-sm">
    {{#each article}}

<div class="jumbotron jumbotron-fluid">
  <div class="container">
    <h1 class="display-4">{{headline}}</h1>
    <p class="lead">{{summary}}</p>
    <a href="{{url}}" class="card-link">Link to Article</a>

  </div>
</div>


    {{/each}}
</div>